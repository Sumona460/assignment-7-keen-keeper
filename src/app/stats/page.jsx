export const metadata = {
  title: "Stats",
  description: "Page",
};

const StatsPage = () => {
    return (
        <div>
            <h2>Stats page ready!</h2>
        </div>
    );
};

export default StatsPage;