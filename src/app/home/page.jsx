export const metadata = {
  title: "Home",
  description: "Page",
};

const HomePage = () => {
    return (
        <div>
            <h2>This is home page</h2>
        </div>
    );
};

export default HomePage;