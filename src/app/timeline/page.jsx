export const metadata = {
  title: "Timeline",
  description: "Page",
};

const TimeLine = () => {
    return (
        <div>
            Timeline page!!
        </div>
    );
};

export default TimeLine;